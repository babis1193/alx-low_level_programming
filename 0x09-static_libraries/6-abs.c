#include "main.h"
/**
* _abs - to  print the absolute value of a number
*@i: targeted number to 
*Return: returns 0
*/
int _abs(int i)
{
	if (i > 0)
	return (i);
	else if (i < 0)
	return (-i);
	else
	return (0);
}
